<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://dubbo.apache.org/schema/dubbo
       http://dubbo.apache.org/schema/dubbo/dubbo.xsd">
    <!-- 配置可参考 http://dubbo.io/User+Guide-zh.htm -->
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="${spring.dubbo.application.name}" owner="${spring.dubbo.application.owner}">
        <dubbo:parameter key="qos.enable" value="true"/>
        <dubbo:parameter key="qos.accept.foreign.ip" value="false"/>
        <dubbo:parameter key="qos.port" value="22226"/>
    </dubbo:application>
    <!-- 定义 zookeeper 注册中心地址及协议 -->
    <dubbo:registry protocol="${spring.dubbo.registry.protocol}" address="${spring.dubbo.registry.address}"/>
    <!-- 生成远程服务代理，可以和本地 bean 一样使用 testService -->
    <!--<dubbo:reference id="sysUserService" interface="com.jww.ump.rpc.api.SysUserService"/>
    <dubbo:reference id="sysDicService" interface="com.jww.ump.rpc.api.SysDicService"/>
    <dubbo:reference id="sysParamService" interface="com.jww.ump.rpc.api.SysParamService"/>-->

    <dubbo:consumer timeout="${spring.dubbo.consumer.timeout}" group="${spring.dubbo.reference.group}"
                    retries="${spring.dubbo.consumer.retries}"/>
</beans>